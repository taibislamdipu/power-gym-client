import React from 'react';
import Header from '../Header/Header';

const MainHeader = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default MainHeader;